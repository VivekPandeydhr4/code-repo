#include <stdio>


int main()
{
  int arr[] = {2, 5, 3, 1, 8, 10, 4, 12, 11, 9};
  int size = sizeof(arr)/sizeof(arr[0]);
  int ar1[size] = {0};
  int i;

  for (i=0; i<size; i++)
    printf("%d ", arr[i]);

  return 0;
}
